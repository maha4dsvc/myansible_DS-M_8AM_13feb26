---
- hosts: all
  become: yes
  tasks: 
  - name: install apache2 webserver ( sudo apt-get install apache2)
    apt:
      name: apache2
      state: present

  - name: i want to start wberver service  (sudo service apache2 start)
    service:
      name: apache2
      state: started
